# Projekt_Telecom_Map

Aby uruchomić w RStudio:

install.packages("shiny")

library(shiny)

runGitHub( "Projekt_Telecom_Map", "PrzemekX")

Użyte biblioteki:

library(shiny)

library(leaflet)

library(jsonlite)
